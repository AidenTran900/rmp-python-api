site_name: RateMyProfessors API
theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]

nav:
  - Home: index.md
  - API Reference:
    - Client: api/client.md
    - Models: api/models.md
    - Scoring: api/scoring.md
